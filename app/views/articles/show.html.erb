<h4>
  The <%= @article.id.ordinalize %> article
</h4>
<p>
  <strong>Title:</strong><br>
  <%= @article.title %>
</p>  

<p>
  <strong>Body:</strong><br>
  <%= @article.text %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>


<h2>Comments</h2>

<% if @article.comments.first != nil %>
  <% @article.comments.each do |comment| %>
    <%= render @article.comments  %> 
  <% end %>
<% else %>
  <h4>There is not comments yet, be the first commenter!</h4>
<% end %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

